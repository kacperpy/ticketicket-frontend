import { Box, Typography } from "@mui/material";
import { centeredBoxStyle } from "../../styles/componentStyles";

export const HomePage = () => {
  return (
    <Box sx={centeredBoxStyle}>
      <Typography>hello</Typography>
    </Box>
  );
};
